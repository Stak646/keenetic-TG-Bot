# Keenetic TG Bot — управление роутером, HydraRoute, NFQWS2, AWG (Entware)

Telegram‑бот для Keenetic (например **Giga KN‑1012**) который запускается **прямо на роутере** в Entware (`/opt`)
и позволяет управлять:

- 🧠 роутером (статус, сеть, DHCP, reboot, базовая диагностика)
- 🧬 **HydraRoute Neo/Classic** (start/stop/restart/status, работа с `domain.conf`, ссылка на HRweb если установлен)
- 🧷 **NFQWS2** (+ **NFQWS web**, если установлен) — сервис, списки, логи, диагностика NFQUEUE
- 🧿 **AWG Manager** — сервис, web‑панель, health‑check, `wg show`
- 📦 **opkg** — update / list‑upgradable / upgrade целевых пакетов
- 🔔 уведомления: падение сервисов, ошибки в логах, интернет down/up, доступные обновления opkg

## Важно
- Бот **не выполняет произвольные команды** из чата — только заранее определённые действия.
- Доступ ограничен по Telegram **user_id** (см. `admins`).
- Все изменения в конфигурациях делаются с бэкапом файлов.

## Быстрый старт (ручная установка)
1) Убедись, что Entware установлен и `/opt` смонтирован.
2) Скопируй файлы проекта на роутер (например в `/opt/tmp/keenetic-tg-bot`).
3) Запусти:
```sh
sh install.sh
```
4) Отредактируй конфиг:
`/opt/etc/keenetic-tg-bot/config.json`
и укажи `bot_token` и `admins` (твой Telegram user_id).

5) Управление сервисом:
```sh
/opt/etc/init.d/S99keenetic-tg-bot status
/opt/etc/init.d/S99keenetic-tg-bot restart
tail -n 200 /opt/var/log/keenetic-tg-bot.log
```

## Автоустановка (рекомендуется)
Скрипт **сам проверяет**, какие модули установлены, и **предлагает установить недостающие**.

### Интерактивно (просто запусти)
```sh
sh autoinstall.sh
```

### Установить всё недостающее без вопросов
```sh
sh autoinstall.sh --yes
```

### Выборочно
```sh
sh autoinstall.sh --bot --cron --weekly
sh autoinstall.sh --hydra --nfqws2 --nfqwsweb --awg
```

## Еженедельные обновления
Опция `--weekly` ставит cron‑задачу на **каждый четверг 06:00** и создаёт:
- `/opt/bin/weekly-update.sh`
- лог `/opt/var/log/weekly-update.log`

## Настройка уведомлений
В `config.json`:
- `monitor.enabled`
- `notify.*`
- пороги нагрузки/места и интервалы

## Безопасность
- Не добавляй лишних людей в `admins`.
- По умолчанию бот принимает команды **только в личке админов** (если `allow_chats` пустой).

## Сделано с помощью ИИ
Проект создан и дорабатывался с помощью **ИИ‑ассистента (ChatGPT)** по запросам автора. Автор отвечает за финальную проверку,
тестирование на своём роутере и использование.

## Лицензия
Добавь лицензию под свои нужды (MIT/Apache‑2.0 и т.д.).
